name: mobilne_automaty_komorkowe
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: any
  flutter_gif: ^0.0.4
  flutter_processing: ^0.2.0
  smooth_page_indicator: ^1.2.1
  url_launcher: ^6.3.1

  flutter_localizations:
    sdk: flutter 

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  lint: ^2.8.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/tlo.gif
    - assets/test.jpg
